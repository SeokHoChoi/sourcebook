10:19
가상 면접 사례로 배우는 대규모 시스템 설계 기초
— System Design

이런 단점 때문에, 대규모 애플리케이션을 지원하는 데는 수평적 규모 확장법이 보다 적절하다.
앞서 본 설계에서 사용자는 웹 서버에 바로 연결된다. 웹 서버가 다운되면 사용자는 웹 사이트에 접속할 수 없다. 또한, 너무 많은 사용자가 접속하여 웹 서버가 한계 상황에 도달하게 되면 응답 속도가 느려지거나 서버 접속이 불가능해질 수도 있다. 이런 문제를 해결하는 데는 부하 분산기 또는 로드밸런서(load balancer)를 도입하는 것이 최선이다.

로드밸런서

로드밸런서는 부하 분산 집합(load balancing set)에 속한 웹 서버들에게 트래픽 부하를 고르게 분산하는 역할을 한다. 그림 1-4는 로드밸런서가 어떻게 동작하는지 보여주고 있다.

사용자 단말
웹 브라우저
모바일 앱
DNS
도메인
IP 주소
mywebsite.com
88.88.88.1
PublicIP: 88.88.88.1
로드밸런서
Private IP: 10.0.0.1
Private IP: 10.0.0.2
서버1
서버2
그림 1-4

그림 1-4와 같이, 사용자는 로드밸런서의 공개 IP 주소(public IP address)로 접속한다. 따라서 웹 서버는 클라이언트의 접속을 직접 처리하지 않는다. 더 나

6
1장 사용자 수에 따른 규모 확장성

19 / 321
듣기
목차
독서노트
보기설정
화면설정

10:35
가상 면접 사례로 배우는 대규모 시스템 설계 기초
— System Design

주 데이터베이스의 수보다 많다. 그림 1-5는 이 구성을 보여주고 있다.
데이터베이스를 다중화하면 다음과 같은 이득이 있다.

웹 서버
쓰기 연산
읽기 연산
읽기 연산
읽기 연산
주 데이터베이스 서버
DB 다중화
DB 다중화
DB 다중화
부 데이터베이스 서버 1
부 데이터베이스 서버 2
부 데이터베이스 서버 3
그림 1-5

• 더 나은 성능: 주-부 다중화 모델에서 모든 데이터 변경 연산은 주 데이터베이스 서버로만 전달되는 반면 읽기 연산은 부 데이터베이스 서버들로 분산된다. 병렬로 처리될 수 있는 질의(query)의 수가 늘어나므로, 성능이 좋아진다.
• 안정성(reliability): 자연 재해 등의 이유로 데이터베이스 서버 가운데 일부가 파괴되어도 데이터는 보존될 것이다. 데이터를 지역적으로 떨어진 여러 장소에 다중화시켜 놓을 수 있기 때문이다.

8
1장 사용자 수에 따른 규모 확장성

21 / 321
듣기
목차
독서노트
보기설정
화면설정
